<a href="#main" class="skip-link">{{ meta.skipContent }}</a>

<header class="wrapper">
  <div class="repel ontop">
    {# Site name + toggle button#}
    <a
      href="/"
      class="logo | cluster"
      {% if page.url == "/" %}aria-current="page"{% endif %}
    >
      <span>{{ meta.siteName }}</span>
    </a>

    {# Page navigation#}
    {% include "partials/main-nav.njk" %}
  </div>
</header>
